apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ciderscope-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: ciderscope.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ciderscope-service
            port:
              number: 80
